
# ─────────────────────────────────────────────────────────────
# BASE DE DATOS (PostgreSQL via Docker)
# ─────────────────────────────────────────────────────────────
# Cadena de conexión completa para Prisma.
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DB_NAME
DATABASE_URL="postgresql://postgres:password@localhost:5432/kyzz-shop"

# Variables auxiliares usadas por docker-compose.yml
DB_USER=postgres
DB_NAME=kyzz-shop
DB_PASSWORD=password

# ─────────────────────────────────────────────────────────────
# NEXT AUTH v5
# ─────────────────────────────────────────────────────────────
# Clave secreta para firmar tokens JWT.
# Genera una con: openssl rand -base64 32
AUTH_SECRET="REEMPLAZA_CON_TU_SECRETO"

# URL base de tu aplicación (sin trailing slash).
# En producción: https://tu-dominio.com
NEXTAUTH_URL="http://localhost:3000"

# ─────────────────────────────────────────────────────────────
# PAYPAL
# ─────────────────────────────────────────────────────────────
# Credenciales de tu app en: https://developer.paypal.com → Apps & Credentials
#
# NEXT_PUBLIC_* es visible en el cliente (solo el Client ID, nunca el Secret)
NEXT_PUBLIC_PAYPAL_CLIENT_ID=""

# Secret de servidor — NUNCA usar prefijo NEXT_PUBLIC_ aquí
PAYPAL_SECRET=""

# URLs de la API de PayPal:
#   Sandbox (desarrollo): https://api-m.sandbox.paypal.com
#   Producción:           https://api-m.paypal.com
PAYPAL_OAUTH_URL="https://api-m.sandbox.paypal.com/v1/oauth2/token"
PAYPAL_ORDERS_URL="https://api-m.sandbox.paypal.com/v2/checkout/orders"

# ─────────────────────────────────────────────────────────────
# CLOUDINARY (almacenamiento de imágenes de productos)
# ─────────────────────────────────────────────────────────────
# Credenciales disponibles en: https://console.cloudinary.com → Dashboard
#
# Formato de CLOUDINARY_URL: cloudinary://API_KEY:API_SECRET@CLOUD_NAME
CLOUDINARY_URL="cloudinary://API_KEY:API_SECRET@CLOUD_NAME"
